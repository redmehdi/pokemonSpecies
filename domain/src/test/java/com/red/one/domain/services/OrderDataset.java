package com.red.one.domain.services;

import com.red.one.domain.entities.PokeSpecies;

import java.util.List;
import java.util.UUID;

public final class OrderDataset {

    /**public static Order getOrder(final UUID id, final List<OrderItem> orderItems, final OrderStatus status, final Long total) {
        Order domain = new Order();
        domain.setId(id);
        domain.setOrderItems(orderItems);
        domain.setStatus(status);
        domain.setTotal(total);
        return domain;
    }

    public static PokeSpecies getDish() {
        PokeSpecies pokeSpecies = new PokeSpecies();
        pokeSpecies.setId(UUID.randomUUID());
        pokeSpecies.setType("food");
        pokeSpecies.setName("Paella");
        pokeSpecies.setIngredients("cebolla, pescado, tomate ...");
        return pokeSpecies;
    }*/
}
